# Docker Nginx Config (no SSL)
server {
    listen 80;
    server_name art.mavoid.com;

    root /usr/share/nginx/html;
    index index.html;
    
    # Frontend routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}